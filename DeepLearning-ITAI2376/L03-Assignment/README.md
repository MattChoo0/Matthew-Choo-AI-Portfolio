In L03, we are tasked to build, train and evaluate Convolutional Neural Networks (CNNs) for binary image classification.
Will be learning how to build CNNs from scratch and leverage transfer learning with pre-trained models like ResNet50 and MobileNetV2 using the viral "Puppy or Bagel" dataset.
In this lab, we have to learn about building CNN architectures from scratch using TensorFlow/Kera, implement data augmentation to improve model generalization, applying transfer learning and fine-tuning using pre-trained models like ResNet50 or MobileNetV2, and comparing the performance of custom models against transfer learning approaches. 
The core tasks and requirements in this lab are using the environment setup by using Google Collab or Amazon Sage Maker Studio Lab with GPU acceleration enabled. You must set up a Kaggle API token to access the dataset. 
Have to complete four specific exercises to build a custom CNN, including adding convolutional blocks, classification layers (Flatten, Dense, Dropout), and compiling the model.
Later on, have to do our specific exercises to build a custom CNN, including adding convolutional blocks, classification layers (Flatten, Dense, Dropout), and compiling the model.
Next, answering the five conceptual questions regarding architecture, data augmentation, and model comparison.
Lastly, have to perform fine-tuning by unfreezing the base model, freezing all but the last 30 layers, and re-training with a lower learning rate (0.0001).
